- @events.each do |event|
  %tr{:'data-event-id' => event.id}
    %td
      = event.title
    %td
      = event.date
    %td
      = event.city
    %td
      = event.state
    %td
      = link_to 'Delete', '#', class: 'delete-button'
      = link_to 'Edit', '#', class: 'edit-button'